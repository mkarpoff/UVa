#!/bin/bash
EXE=ans
TESTFILES=TestFiles/*
for f in $TESTFILES
do
		echo "================================================"
		cat $f;
		echo "================================================"
		./$EXE < $f;
		echo "================================================"
done
