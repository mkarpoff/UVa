#!/bin/bash
EXE=
TESTFILES=
for f in $TESTFILES
do
		echo "================================================"
		cat $f;
		echo "================================================"
		./$EXE < $f;
		echo "================================================"
done
